using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PathCreatureMove : MonoBehaviour
{//This script will have all the movement specific code from path controller moved over eventually.
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
